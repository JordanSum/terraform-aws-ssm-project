# Example Terraform variables file for Multi-Region AWS SSM Project
# Copy this file to terraform.tfvars and update with your actual values
# DO NOT commit terraform.tfvars to version control

# Project configuration
project_name = "ssm_project_terraform"    # Descriptive name for your project
environment = "dev"                       # Environment: dev, staging, prod

# Network configuration (different CIDR blocks for each region)
cidr_block_west = "192.168.0.0/16"       # West region VPC CIDR block
cidr_block_east = "10.0.0.0/16"          # East region VPC CIDR block

# US-West-2 (Oregon) subnets
subnet_one_west = "192.168.20.0/24"      # First subnet in us-west-2a
subnet_two_west = "192.168.30.0/24"      # Second subnet in us-west-2b

# US-East-2 (Ohio) subnets  
subnet_one_east = "10.0.20.0/24"         # First subnet in us-east-2a
subnet_two_east = "10.0.30.0/24"         # Second subnet in us-east-2b

# EC2 configuration (region-specific AMIs required)
ami_id_west = "ami-054b7fc3c333ac6d2"    # Amazon Linux 2023 AMI ID for us-west-2
ami_id_east = "ami-068d5d5ed1eeea07c"    # Amazon Linux 2023 AMI ID for us-east-2
instance_type = "t2.micro"               # EC2 instance type

# Notes:
# - Update ami_id_west with the latest Amazon Linux 2023 AMI for us-west-2
# - Update ami_id_east with the latest Amazon Linux 2023 AMI for us-east-2
# - Ensure CIDR blocks don't conflict with existing VPCs in either region
# - Choose instance_type based on your workload requirements
# - Use descriptive project_name for resource identification across regions
# - Different CIDR blocks are used for each region to enable VPC peering
# - VPC peering requires non-overlapping CIDR ranges

# Example AMI IDs (update with current ones):
# us-west-2: ami-054b7fc3c333ac6d2 (Amazon Linux 2023)
# us-east-2: ami-068d5d5ed1eeea07c (Amazon Linux 2023)

# Multi-Region Architecture:
# - West Region (us-west-2): 192.168.0.0/16 network
# - East Region (us-east-2): 10.0.0.0/16 network
# - VPC Peering: Enabled between regions for cross-region connectivity
# - SSM Agent: Pre-installed on Amazon Linux 2023 AMIs
